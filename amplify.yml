version: 1
frontend:
    phases:
        preBuild:
            commands:
                - rm -rf node_modules
                - npm ci --include=dev
        build:
            commands:
                - npx react-scripts build
    artifacts:
        baseDirectory: build 
        files:
            - '**/*'
    cache:
        paths: []
